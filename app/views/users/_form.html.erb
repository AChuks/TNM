<%= form_for(@user , {class: 'form', role: 'form'}) do |f| %>

  <div class="form-group form-group-custom-margin">
    <%= f.label :user_name, 'Username' %>
    <%= f.text_field :user_name,  :class => 'form-control' %>
  </div>
  <div class="form-group form-group-custom-margin">
    <%= f.label :email, 'Email' %>
    <%= f.text_field :email, :class => 'form-control' %>
  </div>
  <% if !@user.id.nil? %>
    <div class="form-group form-group-custom-margin">
      <%= f.label :current_password, 'Current Password' %>
      <%= f.password_field :current_password, :class => 'form-control' %>
    </div>
  <% end %>
  <div class="form-group form-group-custom-margin">
    <%= f.label :password, @user.id.nil? ? "Password" : "New Password" %>
    <%= f.password_field :password, :class => 'form-control' %>
  </div>
  <div class="form-group form-group-custom-margin">
    <%= f.label :password_confirmation,@user.id.nil? ? "Password Confirmation" : "New Password Confirmation" %>
    <%= f.password_field :password_confirmation, :class => 'form-control' %>
  </div>
  <% if !@user.id.nil? %>
    <div class="form-group form-group-custom-margin">
      <%= f.label :super_admin, 'Super Admin' %>
      <%= f.check_box :super_admin, :class => 'form-control' %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit @user.id.nil? ? "Register" : "Submit", :class => 'btn btn-primary' %>
  </div>

  <% if @user.errors.any? %>
    <div class="alert alert-danger">
      <span><%= pluralize(@user.errors.count, "error") %>: </span>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% end %>
