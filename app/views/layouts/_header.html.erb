<nav class="navbar" role = "navigation">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-id">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
    </div>
    <div class="navbar-container">
        <ul class="nav navbar-nav pull-left col-xs-4">
        <li><%= link_to root_path, {:class => 'navbar-logo'} do %>
            <%= show_svg('super_comedian.svg') %>
            <% end %>
        </li>
        </ul>
        <ul class="nav navbar-nav pull-left col-xs-4">
        <div class='search'>
            <%= form_tag url_for(:controller => 'pages', :action => 'search'), :method => 'get', :id => 'search-form' do %>
                <i class="fa fa-search"></i>
                <%= text_field_tag(:search_text, @search_text) %>
            <% end %>
        </div>
        </ul>
        <div class="collapse navbar-collapse" id="navbar-id">
          <ul class="nav navbar-nav pull-right">
        <li><%= link_to new_video_path, {:class => 'navbar-links'} do %>
            Submit Video
            <% end %>
        </li>
        <li><%= link_to contact_path, {:class => 'navbar-links'} do %>
            Contact Us
            <% end %>
        </li>
         <% if logged_in? %>
            <li>
                <%= link_to videos_path, {:class => 'navbar-links', :method => 'get'} do %>
                Videos
                <% end %>
            </li>
            <li>
                <%= link_to users_path, {:class => 'navbar-links', :method => 'get'} do %>
                 Users
                <% end %>
            </li>
            <li>
                <%= link_to admin_logout_path, {:class => 'navbar-links', :method => 'delete'} do %>
                Log Out
                <% end %>
            </li>
        <% end %>
    </ul>  
        </div>
    </div>
</nav>