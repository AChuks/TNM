<%= form_for(@newest_fashion) do |f| %>
  <% if @newest_fashion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newest_fashion.errors.count, "error") %> prohibited this newest_fashion from being saved:</h2>

      <ul>
      <% @newest_fashion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :mdata %><br>
    <%= f.text_field :mdata %>
  </div>
  <div class="field">
    <%= f.label :img %><br>
    <%= f.text_field :img %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
