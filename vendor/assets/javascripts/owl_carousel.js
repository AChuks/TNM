$(document).ready(function(){
  $('.owl-carousel').owlCarousel({
  		// rtl: true,
  		margin: 10,
  		autoWidth:true,
  		loop:true,

    	autoplay: true,
    	autoplayHoverPause:true,
    	autoplayTimeout:1000,

    	touchDrag: true
  });
});